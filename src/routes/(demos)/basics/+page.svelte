<script lang="ts">
  import { SubpageGrid } from '$lib'
</script>

<SubpageGrid
  title="Basics Overview"
  subtitle="Core examples for standard form usage and event handling."
  subpages={[
    [`Form`, `/form`, `Form integration and native validation behavior.`],
    [`Events`, `/events`, `Event callbacks and payloads.`],
    [`Disabled`, `/disabled`, `Disabled options and disabled component states.`],
  ]}
/>
